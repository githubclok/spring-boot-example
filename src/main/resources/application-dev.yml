## develop configure

server:
  port: 8080

spring:
  datasource:
    username: root
    password: yuwei123
    url: jdbc:mysql://localhost:3306/icbc_cloud_icbc?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
    mapper-locations: classpath:mapping/*Mapper.xml
    type-aliases-package: com.example.demo.entity

#showSql
logging:
    level:
      com:
        example:
          mapper: debug